@page
@model wacdoprojet.Views.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Poste</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Poste.Intituleposte)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Poste.Intituleposte)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Poste.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
